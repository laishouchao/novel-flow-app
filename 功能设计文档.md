# Novel Flow 功能设计文档

## 一、项目概述

### 1.1 产品定位

Novel Flow 是一款专为小说创作者设计的桌面端 AI 辅助写作工具，将小说创作拆解为 5 个阶段的标准化流水线（灵感收束 → 大纲规划 → 正文写作 → 内容审查 → 设定同步），在每个阶段通过 AI 辅助提升创作效率，同时通过严格的质量控制体系确保文本质量。

### 1.2 技术架构

- **前端**: React 18 + TypeScript + Tailwind CSS + Vite
- **桌面容器**: Tauri v1 (Rust)
- **状态管理**: 自定义 Context + useReducer 模式
- **数据持久化**: localStorage (防抖 500ms) + 磁盘文件 (防抖 2000ms)

### 1.3 设计理念

融合两个设计体系：
- **novel-flow 渐进收束法**: Markdown + YAML frontmatter + 双层状态机
- **AI_NovelGenerator 雪花写作法**: 文件系统存储 + 结构化创作流程

---

## 二、核心数据模型

### 2.1 NovelProject 项目模型

```
NovelProject
├── 基础信息: id, name, author, genre, theme, style, targetWords
├── 双层状态机:
│   ├── ProjectStatus: idea → planned → drafting → reviewing → done → blocked
│   └── ProjectStage: brainstorm → outline → draft → review → update
├── 雪花写作法产物: coreSeed, characterDynamics, worldBuilding, plotArchitecture
├── 渐进收束设定: premise, conflict, openingStrategy, nonGoals
├── Canon 变更日志: CanonEntry[] (chapter/volume/change/timestamp)
├── 世界观设定: worldbuilding (JSON), worldFactions (势力/阵营)
├── 伏笔追踪: Foreshadowing[] (planted → echoed → resolved → abandoned)
├── 情节线管理: Storyline[] (introduction → development → complication → climax → resolution)
└── 角色系统: Character[] + CharacterRelation[]
```

### 2.2 Chapter 章节模型

```
Chapter
├── 基础信息: id, title, volumeNumber, chapterNumber, targetWords, wordCount
├── 结构标记: structureTag (setup/build/climax/fallout)
├── 章节状态: status (pending → drafting → reviewing → minor_fix/rewrite → done/rejected)
├── AI 元数据: suspenseLevel(1-5), foreshadowing, plotTwistLevel(1-5)
├── 内容字段: draftContent, finalContent, summary, reviewNotes, finalSummary
└── 时间戳: createdAt, updatedAt
```

### 2.3 Character 角色模型

```
Character
├── 基础信息: id, name, role (protagonist/supporting/antagonist/minor)
├── 角色设定: description, motivation, fear, trait, backstory
├── 角色弧光 (7 阶段):
│   表面追求 → 深层渴望 → 灵魂需求 → 初始状态 → 触发事件 → 蜕变节点 → 最终状态
├── 运行时状态 (CharacterState):
│   ├── items: 持有物品
│   ├── abilities: 能力列表
│   ├── status: 当前状态描述
│   ├── relationships: 与其他角色的关系 (Record<string, string>)
│   └── events: 经历的事件
└── 变更日志: CharacterChange[]
```

---

## 三、功能模块设计

### 3.1 灵感收束 (BrainstormPanel)

**功能目标**: 通过 9 维度渐进式对话，将用户的模糊灵感转化为结构化的项目设定。

#### 9 维度设计

| 序号 | 维度 ID | 维度名称 | 设计意图 |
|------|---------|---------|---------|
| 1 | genreTone | 核心概念 | 确定小说类型和整体基调 |
| 2 | coreConflict | 世界观设定 | 确定核心矛盾驱动力 |
| 3 | protagonist | 主角人设 | 确定主角原型 |
| 4 | worldSetting | 核心冲突 | 确定世界基本规则 |
| 5 | narrativeStructure | 情感基调 | 确定叙事架构 |
| 6 | emotionalCore | 叙事视角 | 确定核心情感命题 |
| 7 | uniqueSellingPoint | 关键转折 | 确定独特卖点 |
| 8 | characterRelationships | 主题深度 | 确定角色关系网 |
| 9 | endgameVision | 独特卖点 | 确定终局愿景 |

#### 核心逻辑流程

1. **渐进提问**: 每个维度 AI 提供 3-4 个具体选项 (A/B/C/D) + 1 个自由发挥选项 (E)
2. **上下文传递**: 用户选择后进入下一维度，已确认信息作为上下文传递给后续提问
3. **流式输出**: 使用 `requestAnimationFrame` 合并流式 token 更新，避免高频重渲染
4. **灵感确认**: 9 维度全部完成后调用 `aiPipeline.brainstormConfirm()` 将所有对话整合为项目方案
5. **数据解析**: 通过正则提取书名/类型/主题/核心种子/世界观/冲突/角色动力学/情节架构/开篇策略/非目标
6. **状态切换**: 用户确认后将数据写入 store，创建默认卷，切换到大纲阶段

#### 输出产物

- `NovelProject.coreSeed`: 一句话故事 (Logline)
- `NovelProject.genre`: 题材类型
- `NovelProject.theme`: 主题
- `NovelProject.worldBuilding`: 世界观设定
- `NovelProject.conflict`: 核心冲突
- `NovelProject.characterDynamics`: 角色动力学
- `NovelProject.plotArchitecture`: 情节架构
- `NovelProject.openingStrategy`: 开篇策略
- `NovelProject.nonGoals`: 非目标 (明确不写什么)

---

### 3.2 大纲规划 (OutlinePanel)

**功能目标**: 将项目方案转化为卷/章两级结构的详细大纲。

#### 结构设计

```
Volume (卷)
└── Chapter (章)
    ├── 章节标题
    ├── 叙事功能
    ├── 结构标记: setup(铺垫) / build(推进) / climax(高潮) / fallout(余波)
    ├── 元数据:
    │   ├── 悬念密度 (1-5 星)
    │   ├── 伏笔操作 (埋设/照应/推进)
    │   └── 认知颠覆强度 (无/低/中/高)
    └── 内容概要 (200-300 字)
```

#### 核心逻辑流程

1. **自动建卷**: 从 store 创建默认卷（如 store 中没有卷数据）
2. **AI 生成**: 调用 `aiPipeline.generateOutlineStream()` 生成整卷大纲
3. **Markdown 解析**: 使用 `parseChaptersFromContent()` 从 AI 返回的 Markdown 中解析章节列表
4. **数据写入**: 解析出的章节通过 `projectActions.addChapter()` 写入 store
5. **用户交互**: 用户可点击章节进入写作台

#### AI 提示词模板

提示词要求大纲具备 6 个特征：
1. 每章有明确的**叙事功能**（不是"发生了什么"，而是"达到什么叙事目的"）
2. 清晰的**结构标记**: setup/build/climax/fallout
3. **悬念管理**: 标注悬念密度和类型（钩子/伏笔/反转/预告）
4. **信息控制**: 标注释放和隐瞒的信息
5. **角色弧线**: 标注角色状态变化
6. **章节衔接**: 前章钩子在后续章节中回应

---

### 3.3 正文写作 (ChapterEditor)

**功能目标**: Markdown 编辑器 + AI 辅助写作，支持续写、扩写、缩写、去AI味、审查。

#### 编辑器功能

- **三种视图模式**: edit(编辑) / preview(预览) / compare(对比)
- **自动保存**: 可配置间隔，默认 30 秒
- **实时字数统计**: 显示当前章节字数和总字数
- **项目文件面板**: 右侧可收起/展开，浏览项目文件

#### AI 辅助操作

| 操作 | 方法 | 功能 |
|------|------|------|
| 生成草稿 | `generateChapterDraft()` | 根据大纲生成完整章节 |
| 审查章节 | `reviewChapterStream()` | AI 审查当前章节 |
| 去AI味 | 内置处理 | 处理 AI 痕迹 |
| 扩写 | 扩展内容 | 扩展内容细节 |
| 缩写 | 精简内容 | 精简内容 |

#### 写作系统核心原则

**1) Scene/Sequel 交替模式**:
- **Scene（场景）**: 目标 → 冲突 → 灾难
- **Sequel（反应）**: 反应 → 困境 → 决定

**2) 去AI味系统（7条规则）**:
1. 禁用词表中所有词汇不得出现
2. 句式多样化（长短交替、陈述疑问感叹混合、主被动交替）
3. 段落长度有变化
4. 不连续 3 段相同句式开头
5. 对话有"人味"（口头禅、停顿、重复、不完整句子）
6. 描写具体（名词和动词，少用形容词和副词）
7. 不用"他/她 + 心理活动"模式，通过行为暗示心理

**3) 四种场景类型**:
- 对话场景（潜台词、信息差、冲突）
- 动作场景（节奏感、细节、后果）
- 心理场景（具体念头和记忆碎片，不抽象概括）
- 环境场景（服务于叙事，不为写而写）

#### 第一章特殊规则

- 前 100 字必须出现钩子
- 前 300 字必须明确主角身份
- 前 500 字必须发生第一个冲突
- 禁止用"楔子""引子"
- 禁止开篇大段环境描写
- 禁止超过 3 个有名有姓角色
- 禁止揭示核心秘密

#### 后续章节规则

- 根据结构标记执行不同叙事任务
- 必须自然承接上一章结尾
- 前 300 字内回应前章钩子
- 不重复前章信息

---

### 3.4 内容审查 (ReviewPanel)

**功能目标**: 四层闸门审查体系，确保文本质量。

#### 四层审查体系

| 层级 | 名称 | 权重 | 审查要点 |
|------|------|------|---------|
| 第一层 | 读者视角审查 | 25% | 开篇吸引力、阅读流畅度、情感投入、悬念与好奇心 |
| 第二层 | 禁用词兜底审查 | 25% | 禁用词命中、句式单调、AI味检查、段落结构、对话质量 |
| 第三层 | Canon 一致性检查 | 30% | 世界观一致性、角色一致性、时间线一致性、空间一致性、伏笔一致性、逻辑一致性 |
| 第四层 | 叙事质量检查 | 20% | Scene/Sequel结构、节奏控制、悬念管理、角色弧线、展示vs告诉 |

#### 审查结论体系

- **pass** (通过): 综合分 >= 8 分
- **minor_fix** (有条件通过): 综合分 >= 6 分
- **rewrite_required** (不通过): 综合分 < 6 分
- **reject** (拒绝): 严重问题

#### 问题分级

- **P0 (必须修改)**: Canon 矛盾、逻辑漏洞、严重AI味
- **P1 (强烈建议修改)**: 禁用词命中、句式单调、节奏不当
- **P2 (建议修改)**: 展示vs告诉、细节优化

---

### 3.5 角色管理 (CharacterPanel)

**功能目标**: 管理角色设定、运行时状态和角色关系。

#### 角色类型

- `protagonist`: 主角
- `supporting`: 配角
- `antagonist`: 反派
- `minor`: 次要角色

#### 角色弧光模型 (7 阶段)

1. **表面追求**: 角色公开宣称的目标
2. **深层渴望**: 角色内心真正想要的
3. **灵魂需求**: 角色成长所需的核心转变
4. **初始状态**: 故事开始时的角色状态
5. **触发事件**: 打破平衡的事件
6. **蜕变节点**: 角色转变的关键时刻
7. **最终状态**: 故事结束时的角色状态

#### 关系系统

10 种关系类型:
- family (家人) / lover (恋人) / friend (朋友)
- enemy (敌人) / mentor (导师) / superior (上级)
- ally (盟友) / rival (对手) / colleague (同事) / other (其他)

支持双向/单向关系，含关系图可视化 (`RelationGraph.tsx`)。

---

### 3.6 世界观设定 (WorldbuildingPanel)

**功能目标**: 结构化管理世界观设定，分 4 大模块。

#### 世界观模块设计

**1. 世界起源**
- 创世神话
- 历史纪元
- 文明现状

**2. 自然环境**
- 地理概况、气候特征
- 生态系统、重要资源
- 自然灾害、特殊地点

**3. 人文环境**
- 政治体系、经济体系
- 文化习俗、宗教信仰
- 科技水平、语言文字
- 种族与人口

**4. 力量体系**
- 体系概述、等级划分
- 特殊能力、限制与代价

#### 势力/阵营管理 (WorldFaction)

属性: name, description, goals, leader, territory, hostility (friendly/neutral/hostile)

---

### 3.7 其他功能模块

#### 设定管理 (OutlineToolsPanel)

- **伏笔管理**: 创建/跟踪伏笔状态 (planted → echoed → resolved → abandoned)
- **情节线管理**: 管理多条情节线的进度和阶段
- **势力管理**: 管理世界观中的势力/阵营

#### 提示词管理 (PromptManagerPanel)

- 查看系统提示词
- 管理自定义提示词模板
- 配置任务模型分配

#### 导出功能 (ExportPanel)

- 导出为 Markdown 文件
- 导出项目元数据
- 打包导出完整项目

---

## 四、AI Pipeline 设计

### 4.1 方法清单

| 方法 | 任务类型 | 功能 | 流式支持 |
|------|---------|------|---------|
| `brainstormNextQuestionStream()` | brainstorm | 生成下一个维度的问题 | 是 |
| `brainstormConfirm()` | brainstorm | 整合所有维度回答为项目设定 | 否 |
| `generateOutlineStream()` | outline | 生成整卷大纲 | 是 |
| `generateChapterDraft()` | draft | 生成章节草稿 | 是 |
| `reviewChapterStream()` | review | 综合审查章节 | 是 |
| `updateProjectState()` | finalization | 定稿后同步更新项目设定 | 否 |
| `generateChapterSummary()` | summary | 生成章节摘要 (200-300字) | 否 |
| `generateGlobalSummary()` | summary | 基于新章节更新全局摘要 | 否 |
| `consistencyCheck()` | general | Canon 一致性检查 | 否 |

### 4.2 提示词模板结构

每个提示词模板采用 `{变量}` 占位符参数化，通过 `fillPromptTemplate()` 进行变量替换。

**模板组织**:
- 每个任务阶段一个独立文件 (brainstorm.ts / outline.ts / draft.ts / review.ts / finalization.ts)
- 每个文件包含: 系统提示词 (system prompt) + 多个用户提示词模板
- `prompts/index.ts` 作为统一注册中心，按任务类型映射系统提示词

### 4.3 流式输出机制

```
AIPipeline
    ↓ 调用
LLMService.chatStream()
    ↓ 通过 Tauri
invoke('http_request_stream')
    ↓ Rust 后端处理 SSE 流
emit('stream-chunk', ...)
    ↓ 推送到前端
listen('stream-chunk', ...)
    ↓ 解析
choices[0].delta.content
    ↓ 提取 token
options.onToken(delta)
    ↓ 完成
cleanThinkTags(fullContent)
options.onComplete()
```

### 4.4 上下文管理策略

| 阶段 | 上下文内容 |
|------|-----------|
| 灵感收束 | 已确认的历史消息摘要 (每条截取前 200 字) |
| 大纲生成 | 项目方案 + 角色设定 + 前卷概要 |
| 章节写作 | 角色信息 + 角色当前状态 + 全局摘要 + 前章摘要 + 前章末尾 500 字 + 伏笔清单 |
| 章节审查 | Canon 设定 + 角色设定 + 前文摘要 + 章节内容 |
| 定稿同步 | 全局摘要 (压缩到 2000 字) + 角色状态增量更新 + Canon 变更日志 |

---

## 五、写作风格引擎

### 5.1 风格接口 (StyleRule)

每种风格包含 10 个维度:
- `id`, `name`, `description`, `representatives` (代表作家/作品)
- `sentenceRules` (句式规则)
- `descriptionRules` (描写规则)
- `dialogueRules` (对话规则)
- `emotionRules` (情绪表达规则)
- `forbiddenPatterns` (禁用句式模式)
- `forbiddenWords` (风格专属禁用词)
- `pacing` (节奏: avgSentenceLength / paragraphLength / dialogueRatio)
- `systemPrompt` (完整风格指导提示词，含正反例句)

### 5.2 三种预设风格

| 风格 | ID | 代表 | 核心理念 | 平均句长 | 对话占比 |
|------|-----|------|---------|---------|---------|
| 冷白描 | `coldRealism` | 余华/海明威/鲁迅 | 只写事实不写感受，克制最高级 | 10-15字 | 40-50% |
| 系统爽文 | `systemPower` | 全球高武/超神机械师 | 节奏快/信息密度高/爽点密集 | 15-25字 | 30-40% |
| 怪诞悬疑 | `bizarreSuspense` | 规则怪谈/克苏鲁 | 日常中渗透异常，平静描述恐怖 | 12-20字 | 25-35% |

### 5.3 风格嵌入机制

`embedStyleRules()` 函数将风格规则嵌入写作提示词，包含:
- 完整 systemPrompt
- 生成后自查清单 (句式/描写/对话/情绪/节奏各前 3 条)

---

## 六、禁用词系统

### 6.1 禁用词分类 (8 大类, 170+ 词)

| 分类 | 核心问题 | 示例 |
|------|---------|------|
| 情绪直述 | AI 喜欢直接说出情绪 | 心中一紧、心中一震、心中五味杂陈 |
| 过度修饰 | AI 倾向堆砌形容词 | 宛如、犹如、仿佛、恰似 |
| 陈腔滥调 | 网文套路化表达 | 嘴角微微上扬、眼中闪过一丝 |
| 动作套路 | AI 反复使用的动作 | 不由得、不由自主、下意识、缓缓开口 |
| 转折连接 | 过度使用的连接词 | 然而、话虽如此、与此同时、说时迟那时快 |
| 感官描写套路 | 反复使用的感官描写 | 一股寒意、一股暖流、空气中弥漫着 |
| 心理描写套路 | 内心独白模式 | 暗自思忖、暗自庆幸、不禁莞尔 |
| 环境描写套路 | 环境描写模式 | 夕阳西下、夜幕降临、月色如水 |

### 6.2 三层防线机制

1. **事前告知**: 在写作提示词中注入禁用词列表
2. **风格叠加**: 每种风格有专属禁用词和禁用模式
3. **事后审查**: 第二层审查中的逐条禁用词检查 + AI味模式匹配

---

## 七、数据流设计

### 7.1 持久化机制

**双层持久化**:

| 层级 | 存储位置 | 防抖时间 | 保存内容 |
|------|---------|---------|---------|
| 本地存储 | localStorage | 500ms | 项目元数据、AI 配置、UI 偏好 |
| 磁盘存储 | 文件系统 | 2000ms | 项目元数据 + 章节 .md + 角色 .md |

**磁盘文件结构**:
```
{storagePath}/
├── project.json          # 项目元数据 + 角色 + 卷 + 灵感消息 + 全局摘要
├── chapters/
│   ├── 001_章节标题.md    # YAML frontmatter + 正文
│   └── ...
└── characters/
    ├── 角色名.md          # 角色设定 Markdown
    └── ...
```

### 7.2 状态管理层级设计

```
AppState (React Context + useReducer)
├── ProjectState    # 项目/卷/章节/角色/关系/摘要/灵感消息
├── EditorState     # 当前章节/编辑内容/视图模式/脏标记/审查结果
├── AIState         # LLM 配置/活跃模型/生成状态/流缓冲/事件列表
└── UIState         # 当前视图/侧边栏/对话框/通知/全屏/编辑器偏好
```

**Reducer 架构**:
- 每个域有独立的 reducer 函数
- 根 reducer `appReducer` 按 `domain` 字段路由到子 reducer
- Action 采用 `{ domain, action }` 双层结构

### 7.3 组件间数据传递

```
AppProvider (Context)
    ├── useAppState()      → 读取全局状态
    ├── useAppDispatch()   → 派发 Action
    └── useAppStore()      → 同时获取状态和分发器
```

**配置桥接**: `AppLayout` 组件通过 `llmService.setConfigProvider()` 将 store 中的 AI 配置桥接到 LLM 服务层，实现配置的双向同步。

---

## 八、LLM 服务层设计

### 8.1 核心功能

- `LLMConfigManager`: 支持多配置管理，不同任务类型使用不同模型
- `LLMService.chat()`: 非流式调用，通过 Tauri `invoke('http_request')` 绕过 CORS
- `LLMService.chatStream()`: 流式调用，通过 Tauri `invoke('http_request_stream')` + 事件监听
- `chatWithRetry()`: 指数退避重试 (最多 3 次)
- `testConnection()`: 连接测试

### 8.2 配置管理

支持按任务类型 (`TaskModelAssignment`) 指派不同 LLM 模型:
- brainstorm: 灵感收束
- outline: 大纲生成
- draft: 正文写作
- review: 内容审查
- general: 通用任务

### 8.3 错误处理

`LLMServiceError` 包含:
- `code`: 错误代码
- `statusCode`: HTTP 状态码
- `retryable`: 是否可重试
- `message`: 错误信息

---

## 九、项目生命周期

### 9.1 状态流转

```
idea (灵感阶段)
    ↓ [完成灵感收束]
planned (规划阶段)
    ↓ [完成大纲规划]
drafting (写作阶段)
    ↓ [完成章节写作]
reviewing (审查阶段)
    ↓ [通过审查]
done (完成)
```

### 9.2 阶段流转

```
brainstorm (灵感收束)
    ↓ [用户确认项目方案]
outline (大纲规划)
    ↓ [用户点击章节进入写作]
draft (正文写作)
    ↓ [用户触发审查]
review (内容审查)
    ↓ [审查通过]
update (设定同步)
    ↓ [自动更新全局摘要/角色状态/Canon]
draft (继续下一章)
```

---

## 十、设计哲学总结

1. **渐进式收束**: 从 9 个维度的宽泛提问逐步聚焦为具体方案
2. **结构化元数据**: 大纲层标注 5 种元数据，写作层可精确执行
3. **三层去AI味防线**: 事前禁用词告知 → 风格规则叠加 → 事后四层审查
4. **增量更新机制**: 角色状态只更新变化部分，全局摘要只更新差异
5. **加权评分审查**: 四层审查各维度独立评分，按权重计算综合分
6. **流式架构**: 支持 SSE 流式输出，通过 Tauri 后端命令绕过 CORS 限制
7. **双层持久化**: localStorage + 磁盘文件，保障数据安全
8. **分任务模型指派**: 不同创作阶段使用不同的 LLM 模型
